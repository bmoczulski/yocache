## Environment variables

Everything the server can be told, without ever touching the command. Set any
of these with `-e NAME=value`, or a Compose `environment:` block; anything left
unset falls back to the default shown.
